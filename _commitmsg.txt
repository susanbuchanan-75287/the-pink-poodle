Add staff roles, structured vaccinations, immutable ledger/void, multi-phone spa CRM, real Twilio SMS blast + STOP webhook, closed date ranges, and credential canary

Spa console (spa.js/spa.html):
- Role-based access (owner/manager/stylist) via spaLogin; sensitive actions gated with data-min-role; personal stylist PINs
- Structured per-pet vaccinations {type,expires,verifiedAt,notes} with server-computed status badges; "Vax due" reminder tab; required-vax config
- Multi-phone client editor (primary derived); void flow for paid tickets
- CSV/vCard exports updated for phones + vaccinations

Salon console (admin.js/admin.html):
- Closed date ranges (holidays/vacations) in the schedule editor
- Staff accessRole select + set/clear personal PIN (staffSetPin)
- Real Twilio SMS promo blast (smsBlast) with live textable count, per-customer smsOptIn toggle, graceful not-configured fallback

Backend (functions/index.js, already deployed):
- resolveSpaActor/requireRole; spaLogin; role-gated spa actions
- cleanVax/petVaxStatus/loadRequiredVax; spaVaxConfig(+Save)/spaVaxDue
- cleanPhones for spa clients; upsertClientFromBooking returns clientId
- reverseLedger + spaVoid (append-only ledger); spaDelete refuses paid tickets
- sendSms/smsCount/smsBlast; pinkPoodleSms inbound STOP/START/HELP webhook
- staffSetPin (admin) / spaStaffPin (owner); closedRanges validation
- Rex-Loves-Susan- canary prefix on emailed credential copies

README documents every feature above.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
